import { isPropertySignature } from "typescript";

interface BarProp {
    title: string;
    subtitle: string;
  }

function Bar(prop: BarProp) {
    return (
      <div className="bar">
        <
      </div>
    );
  }
  
  export default Bar;
  
"use strict";
exports.__esModule = true;
var mongoose = require("mongoose");
var helpModel = new mongoose.Schema({
    name: String,
    id: paragraph
});
//the collection
var Owners = mongoose.model("Owners", OwnerSchema);
exports["default"] = Owners;
